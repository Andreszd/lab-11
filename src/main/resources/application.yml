spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/spring_lab_11_db?serverTimezone=UTC
    username: joseandre
    password: 12345
    initialization-mode: always
